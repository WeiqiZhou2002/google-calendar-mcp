timezone: America/Chicago         
actions:
  read:
    enabled: true
    max_future_days: 30       

  write:                          
    enabled: true
    max_future_days: 7

  delete:                         
    enabled: false

calendars:
  whitelist:                      # optional - default to all
    - primary
